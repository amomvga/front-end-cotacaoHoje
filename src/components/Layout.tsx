import HeaderTitle from "./HeaderTitle";
import List from "./List";

export default function Layout() {
  return (
    <div>
      <HeaderTitle title="Cotação Hoje" />
      <List />
    </div>
  );
}
